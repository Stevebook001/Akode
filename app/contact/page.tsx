"use client";
import {useState} from "react";
export default function Contact(){const [status,setStatus]=useState("");async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Sending…");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/contact",{method:"POST",body:JSON.stringify(Object.fromEntries(form)),headers:{"content-type":"application/json"}});const d=await r.json();setStatus(r.ok?"Message sent successfully.":d.error||"Could not send.");if(r.ok)e.currentTarget.reset();}catch{setStatus("Could not reach the contact service.")}}return <main><div className="eyebrow">Get in touch</div><h1>Contact Ibrahim</h1><p className="lead">Messages are routed to Novella Matrix.</p><form onSubmit={send}><input name="name" placeholder="Your name" required/><input name="email" type="email" placeholder="Your email" required/><textarea name="message" placeholder="Your message" required/><button className="button">Send message</button></form>{status&&<p className="muted">{status}</p>}</main>}