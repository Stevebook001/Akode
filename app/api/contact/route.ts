export const runtime="nodejs";
export async function POST(req:Request){try{const {name,email,message}=await req.json();if(!name||!email||!message)return Response.json({error:"Name, email and message are required."},{status:400});const key=process.env.RESEND_API_KEY;const to=process.env.CONTACT_EMAIL||"novellamatrix@gmail.com";const from=process.env.CONTACT_FROM_EMAIL||"onboarding@resend.dev";if(!key)return Response.json({error:"RESEND_API_KEY is not configured on the server."},{status:503});const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${key}`},body:JSON.stringify({from,to,reply_to:email,subject:`Website contact from ${name}`,text:`Name: ${name}\nEmail: ${email}\n\n${message}`})});const data=await r.json();if(!r.ok)return Response.json({error:data?.message||"Email provider rejected the request."},{status:r.status});return Response.json({ok:true});}catch{return Response.json({error:"Server error."},{status:500})}}