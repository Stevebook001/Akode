"use client";
import {useState} from "react";
export default function AI(){const [q,setQ]=useState("");const [a,setA]=useState("");const [busy,setBusy]=useState(false);async function run(e:React.FormEvent){e.preventDefault();setBusy(true);setA("");try{const r=await fetch("/api/ai",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({message:q})});const d=await r.json();setA(d.answer||d.error||"No response.");}catch{setA("The AI service could not be reached.")}finally{setBusy(false)}}return <main><div className="eyebrow">Private server route</div><h1>AI Assistant</h1><p className="lead">Your OpenAI credential stays on the server. The browser only talks to this website.</p><form onSubmit={run}><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask something..." required/><button className="button" disabled={busy}>{busy?"Thinking…":"Ask AI"}</button></form>{a&&<article className="card" style={{marginTop:24,whiteSpace:"pre-wrap"}}>{a}</article>}</main>}