import { posts } from "@/lib/posts";
export const metadata = { title: "Notes & Articles | Ibrahim Akanni Ahmad", description: "Engineering notes, founder essays, AI, SEO, publishing and author discovery." };
export default function Notes(){return <main className="page"><p className="eyebrow">NOTES FROM THE WORKBENCH</p><h1>I write what I learn.</h1><p className="lead">Technical decisions, product lessons, AI, SEO, publishing, author discovery and the occasional reflection on what it takes to keep building.</p><div className="grid">{posts.slice(0,12).map((p,i)=><article className="card" key={p.slug}><p className="eyebrow">{String(i+1).padStart(2,"0")} · {p.category}</p><h2>{p.title}</h2><p>{p.excerpt}</p><a href={`/blog/${p.slug}`}>Read the note →</a></article>)}</div><section className="feature"><h2>Current editorial direction</h2><p>The library intentionally mixes first-hand project documentation with practical technology, SEO, AI and publishing topics. Current or time-sensitive claims are dated and linked to their sources where appropriate.</p></section></main>}
