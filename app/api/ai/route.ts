export const runtime="nodejs";
export async function POST(req:Request){try{const {message}=await req.json();if(!message||typeof message!=="string")return Response.json({error:"Message is required."},{status:400});const key=process.env.OPENAI_API_KEY;if(!key)return Response.json({error:"OPENAI_API_KEY is not configured on the server."},{status:503});const r=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${key}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-4o-mini",messages:[{role:"system",content:"You are the helpful AI assistant for Ibrahim Akanni Ahmad's official website. Be concise, accurate and professional."},{role:"user",content:message}],temperature:.7})});const data=await r.json();if(!r.ok)return Response.json({error:data?.error?.message||"OpenAI request failed."},{status:r.status});return Response.json({answer:data?.choices?.[0]?.message?.content||"No answer returned."});}catch{return Response.json({error:"Invalid request or server error."},{status:500})}}